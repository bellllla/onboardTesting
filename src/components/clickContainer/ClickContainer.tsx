import React from 'react'
import styles from './ClickContainer.module.css';

function ClickContainer({}):JSX.Element {
    return (
        <section className={styles.section_3}>
            <button className = {styles.click_button}> Learn More </button>
        </section>
    )
}

export default ClickContainer
